{
	"name": "cloud-app",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"install:all": "npm install && npm install --prefix frontend && npm install --prefix backend",
		"build": "npm run build --prefix frontend",
		"start": "cross-env NODE_ENV=production node backend/server.js",
		"azure-postbuild": "npm run build",
		"deploy": "npm run build && npm start"
	},
	"type": "module",
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"bcryptjs": "^3.0.2",
		"cors": "^2.8.5",
		"dotenv": "^16.4.7",
		"express": "^4.21.2",
		"jsonwebtoken": "^9.0.2",
		"mongoose": "^8.12.1",
		"nodemailer": "^6.10.0"
	},
	"devDependencies": {
		"cross-env": "^7.0.3",
		"nodemon": "^3.1.4"
	},
	"engines": {
		"node": "20.x",
		"npm": "10.x"
	}
}